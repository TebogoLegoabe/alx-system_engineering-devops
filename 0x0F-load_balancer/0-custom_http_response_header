#!/usr/bin/env bash

# Configure Nginx so that its HTTP response contains a custom header (on web-01 and web-02)
# The name of the custom HTTP header must be X-Served-By
# The value of the custom HTTP header must be the hostname of the server Nginx is running on

# Install Nginx
sudo apt-get update
sudo apt-get -y install nginx

# Configure the firewall to allow request through port 80
sudo ufw allow 'Nginx HTTP'

# Create a directory for the web server's files
sudo mkdir -p /var/www/html

# Change the permissions on the directory so that we can easily create files in it
sudo chmod -R 755 /var/www

# Create an index page
echo 'Hello World!' |sudo tee  /var/www/html/index.html

# Create a webpage for error 404
echo "Ceci n'est pas une page" |sudo tee /var/www/html/404.html

# Code to configure redirect for a single page(/redirect_me) and add header to display hostname
string_for_replacement="server_name [YOUR_SERVER_HOSTNAME];\n\tadd_header X-Served-By \$hostname;"
sudo sed -i "s/server_name _;/$string_for_replacement/" /etc/nginx/sites-enabled/default

# Restart the web server after updating the settings
sudo service nginx restart
